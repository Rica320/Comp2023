import ioPlus;
IfWhileNested {

    .method public static main(args.array.String).V {

            if (3.i32 >=.bool 4.i32) goto then;
                invokestatic(ioPlus, "printResult", 0.i32).V;
                goto endif;
            then:
                 invokestatic(ioPlus, "printResult", 1.i32).V;
            endif:
                ret.V;
    }

}