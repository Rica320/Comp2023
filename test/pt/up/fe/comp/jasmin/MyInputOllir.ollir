import io;

AssignInnerScope {

.construct AssignInnerScope().V {
invokespecial(this, "<init>").V;
}

.method public static main(args.array.String).V {

                a.i32 :=.i32 5.i32;

                if (a.i32 <.bool 0.i32) goto THEN;

                invokestatic(io, "println", 2.i32).V;
                goto END;
          THEN:
                invokestatic(io, "println", 1.i32).V;
          END:
                ret.V;
    }

}